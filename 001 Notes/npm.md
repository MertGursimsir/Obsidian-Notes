package.json -> user creates
    istenen sürüm aralığı
    ne yüklemek istiyorum?
    ana paketler görülür
package-lock.json -> npm creates
    tam ve kesin sürüm
    ne yükledim?
    ana paketlerin bağımlılıkları da görünür
    şu paket, şu URL'den, şu hash ile kurulu bilgisi var

node_modules içindeki paketlerin tam listesi package-lock.json'da tutulur.