There is meter for each CPU core at top.
Swp shows how much swap is used / total. (swap is at disk)

shift + p --> sort process list by cpu usage
shift + m --> sort process list by memory usage

u --> user
	narrow down processes by user
f3 --> search by name